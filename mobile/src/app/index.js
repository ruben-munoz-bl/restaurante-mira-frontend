import { Placeholder } from '../components/shell/Placeholder';

export default function () {
  return <Placeholder titulo="Inicio" descripcion="Home: Hero + buscador + lista de restaurantes (Fase 3)." />;
}
