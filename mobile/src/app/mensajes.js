import { AppShell } from '../components/shell/AppShell';
import { Placeholder } from '../components/shell/Placeholder';

export default function Mensajes() {
  return (
    <AppShell>
      <Placeholder titulo="Mensajes" descripcion="Bandeja de mensajes (Fase 5)." />
    </AppShell>
  );
}
