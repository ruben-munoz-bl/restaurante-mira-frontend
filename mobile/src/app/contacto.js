import { AppShell } from '../components/shell/AppShell';
import { Placeholder } from '../components/shell/Placeholder';

export default function Contacto() {
  return (
    <AppShell>
      <Placeholder titulo="Contacto" descripcion="Formulario de contacto (Fase 4)." />
    </AppShell>
  );
}
