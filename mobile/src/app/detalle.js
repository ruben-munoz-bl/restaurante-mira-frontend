import { Placeholder } from '../components/shell/Placeholder';

export default function () {
  return <Placeholder titulo="Detalle del restaurante" descripcion="Ficha + reservar + reseñas (Fase 3)." />;
}
